# This is a Databricks asset bundle definition for dabproject.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dabproject
  uuid: eed27f98-8996-418f-addc-69b92f7a9239

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: production
    default: true
    presets:
      name_prefix: dev_
      source_linked_deployment: false
    workspace:
      host: https://adb-183199289847435.15.azuredatabricks.net/
      root_path: /Workspace/Users/nidhi.gupta14@ctpsandbox.com/DEV/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: adx_ni
      schema: default
  prod:
    mode: production
    presets:
      name_prefix: dev_
      source_linked_deployment: false
    workspace:
      host: https://adb-183199289847435.15.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/nidhi.gupta14@ctpsandbox.com to make sure we only have a single copy.
      root_path: /Workspace/Users/nidhi.gupta14@ctpsandbox.com/PROD/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: adx_ni
      schema: default
    permissions:
      - group_name: admins
        level: CAN_MANAGE
